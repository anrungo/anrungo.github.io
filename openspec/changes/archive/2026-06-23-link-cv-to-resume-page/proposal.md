## Why

The home page CV links currently point at a local file (`assets/cv.pdf`), but Antonio maintains an always-up-to-date résumé as a web page at `https://anrungo.github.io/resume/`. Visitors should reach the canonical résumé page instead of a stale local PDF.

## What Changes

- Hero CTA button: change target from `assets/cv.pdf` to `https://anrungo.github.io/resume/`, and relabel "Download CV" → "Curriculum Vitae" (it now opens a page, not a download). It opens in a new tab (`target="_blank"`).
- About section: **remove** the sentence "My full CV is available [here](assets/cv.pdf)." entirely — the résumé is now reachable from the hero CTA, so this line is redundant.
- Re-render the Quarto site so the generated `_site/index.html` reflects the changes.

## Capabilities

### New Capabilities

_None._

### Modified Capabilities

- `landing-page`: the hero CV CTA requirement changes — the button now links to the résumé **page** (`https://anrungo.github.io/resume/`) and is labelled "Curriculum Vitae" instead of "Download CV" pointing at a local PDF.

## Impact

- Source: `index.qmd` (hero CTA line relinked/relabelled; About-section CV sentence removed).
- Generated output: `_site/index.html` (regenerated by Quarto render).
- The local `assets/cv.pdf` is no longer referenced by the home page (kept as-is, not deleted).
- Deploy: requires Quarto re-render and push to the deploy repo to go live.
